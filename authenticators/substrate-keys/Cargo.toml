[package]
authors.workspace = true
description = "Substrate keys authenticator for FRAME Contrib's pallet-pass: registers sr25519/ed25519/ecdsa public keys as devices and verifies their signatures."
edition.workspace = true
keywords = ["polkadot", "substrate", "frame", "signature", "authentication"]
categories = ["authentication", "cryptography", "no-std"]
license.workspace = true
name = "pass-authenticators-substrate-keys"
readme.workspace = true
repository.workspace = true
version.workspace = true

[dependencies]
codec.workspace = true
log.workspace = true
scale-info = { workspace = true, optional = true }
sp-core = { workspace = true, optional = true }
sp-runtime = { workspace = true, optional = true }
traits-authn.workspace = true

[dev-dependencies]
frame.workspace = true
pallet-balances.workspace = true
pallet-pass.workspace = true
pallet-scheduler.workspace = true

[features]
default = ["std", "runtime", "full-crypto"]
runtime = [
  "dep:scale-info",
  "dep:sp-core",
  "dep:sp-runtime",
  "traits-authn/runtime",
]
full-crypto = ["runtime", "sp-core/full_crypto"]
std = [
  "codec/std",
  "frame/std",
  "log/std",
  "pallet-balances/std",
  "pallet-pass/std",
  "pallet-scheduler/std",
  "scale-info?/std",
  "sp-core?/std",
  "sp-runtime?/std",
  "traits-authn/std",
]
try-runtime = [
  "pallet-pass/try-runtime",
  "pallet-balances/try-runtime",
  "pallet-scheduler/try-runtime",
  "frame/try-runtime",
  "sp-runtime/try-runtime",
]
