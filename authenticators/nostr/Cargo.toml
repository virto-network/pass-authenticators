[package]
authors.workspace = true
description = "Nostr authenticator for FRAME Contrib's pallet-pass: registers Nostr keys as devices and verifies their BIP-340 Schnorr signatures."
edition.workspace = true
keywords = ["polkadot", "substrate", "nostr", "schnorr", "authentication"]
categories = ["authentication", "cryptography", "no-std"]
license.workspace = true
name = "pass-authenticators-nostr"
readme.workspace = true
repository.workspace = true
version.workspace = true

[dependencies]
codec.workspace = true
k256 = { workspace = true, optional = true }
log.workspace = true
scale-info = { workspace = true, optional = true }
sp-core = { workspace = true, optional = true }
sp-io = { workspace = true, optional = true }
sp-runtime = { workspace = true, optional = true }
traits-authn.workspace = true

[dev-dependencies]
frame.workspace = true
pallet-balances.workspace = true
pallet-pass.workspace = true
pallet-scheduler.workspace = true

[features]
default = ["std", "runtime", "full-crypto"]
runtime = [
  "dep:k256",
  "dep:scale-info",
  "dep:sp-core",
  "dep:sp-io",
  "dep:sp-runtime",
  "traits-authn/runtime",
]
full-crypto = ["runtime", "sp-core/full_crypto", "k256/arithmetic"]
std = [
  "codec/std",
  "frame/std",
  "k256?/std",
  "log/std",
  "pallet-balances/std",
  "pallet-pass/std",
  "pallet-scheduler/std",
  "scale-info?/std",
  "sp-core?/std",
  "sp-io?/std",
  "sp-runtime?/std",
  "traits-authn/std",
]
try-runtime = [
  "pallet-pass/try-runtime",
  "pallet-balances/try-runtime",
  "pallet-scheduler/try-runtime",
  "frame/try-runtime",
  "sp-runtime/try-runtime",
]
