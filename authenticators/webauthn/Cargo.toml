[package]
authors.workspace = true
description = "WebAuthn (passkey) authenticator for FRAME Contrib's pallet-pass: registers credentials as devices and verifies their assertions."
edition.workspace = true
keywords = ["polkadot", "substrate", "webauthn", "passkey", "authentication"]
categories = ["authentication", "cryptography", "no-std"]
license.workspace = true
name = "pass-authenticators-webauthn"
readme.workspace = true
repository.workspace = true
version.workspace = true

[dependencies]
base64 = { workspace = true, features = ["alloc"] }
bitflags = { workspace = true, optional = true }
byteorder = { workspace = true, optional = true }
codec.workspace = true
frame = { workspace = true, optional = true }
log.workspace = true
coset = { workspace = true, optional = true }
scale-info.workspace = true
serde = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
traits-authn.workspace = true
url.workspace = true
webauthn-verifier.workspace = true

[dev-dependencies]
env_logger = "0.11.8"
coset.workspace = true
futures.workspace = true
pallet-balances.workspace = true
pallet-pass.workspace = true
pallet-scheduler.workspace = true
passkey-authenticator.workspace = true
passkey-client.workspace = true
passkey-types.workspace = true
public-suffix.workspace = true

[features]
default = ["std", "runtime"]
runtime = [
  "dep:frame",
  "frame/runtime",
  "traits-authn/runtime",
  "bitflags",
  "byteorder",
  "coset",
  "serde",
  "serde_json",
]
std = [
  "base64/std",
  "bitflags/std",
  "byteorder/std",
  "codec/std",
  "coset/std",
  "frame?/std",
  "futures/std",
  "log/std",
  "pallet-balances/std",
  "pallet-pass/std",
  "pallet-scheduler/std",
  "scale-info/std",
  "serde?/std",
  "serde_json?/std",
  "traits-authn/std",
  "url/std",
  "webauthn-verifier/std",
]
try-runtime = [
  "frame/try-runtime",
  "pallet-balances/try-runtime",
  "pallet-pass/try-runtime",
  "pallet-scheduler/try-runtime",
]
